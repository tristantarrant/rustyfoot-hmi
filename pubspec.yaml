name: rustyfoot_hmi
description: "Rustyfoot HMI"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image: ^4.7.2
  logging: ^1.3.0
  network_info_plus: ^7.0.0
  qr_flutter: ^4.1.0
  rdflib:
    path: packages/rdflib
  dart_periphery: ^0.9.20
  ffi: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
     - assets/
